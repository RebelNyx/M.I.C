console.log("LOG IN");

var modal=  $('#myModal');




 modal.css("display" , "block");

 $('#form_show').html("<%= j (render 'new') %>")


// Get the <span> element that closes the modal
var span = $('#close');

// When the user clicks on <span> (x), close the modal
span.on('click',function(){

  modal.css("display" , "none");
});