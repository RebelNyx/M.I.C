console.log("follow");

// var follow = $("#follow_<%= @followee_id %>").html();

// console.log(follow);

<% if @follow %>

	$("#follow_<%= @followee_id %>").val("UnFollow");
	$("#follow_show").load(location.href + " #follow_show"); 
	// reload particular div using window loacation object alia of ajax
	// ajax url () , datatype:html succesee .html(html) imp ajax static
	//for dynamic use render

<% else %>
 
 $("#follow_<%= @followee_id %>").val("Follow");

 $("#follow_show").load(location.href + " #follow_show");

 <% end %>




